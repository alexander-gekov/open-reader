{
  "name": "open-reader",
  "version": "1.0.0",
  "description": "Open Reader - A monorepo application",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter nuxt dev",
    "build": "pnpm --filter nuxt build",
    "preview": "pnpm --filter nuxt preview",
    "postinstall": "pnpm --filter nuxt postinstall",
    "clean": "pnpm --filter nuxt clean",
    "lint": "pnpm --filter nuxt lint",
    "type-check": "pnpm --filter nuxt type-check",
    "backend:run": "cd apps/backend && go run ."
  },
  "devDependencies": {
    "@types/node": "^20.11.0"
  },
  "packageManager": "pnpm@8.15.0"
}
